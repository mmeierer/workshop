# 1. Install and load the data.table package (use RStudio's code editor).
install.packages("data.table")
library(data.table)

# 2. Find and use an R command to list all installed packages (use RStudio's code editor).
installed.packages()

# 3. Have a look at the help file for the R command library().
# What are optional arguments when loading a package?
help(library)
?library
# an alternative argument to load a package is require()
