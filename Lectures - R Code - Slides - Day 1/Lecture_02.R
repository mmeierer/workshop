# Use the code editor in RStudio to run R code 
print("This is my first line of code.")

# How to install R packages
install.packages("data.table")

# How to load/activate an R package
library(data.table)

# Sidenote
?fread
help(stats)
example(fread)
args(fread)
help.search("fread")
??"ggplot2"

# Remove an R package
remove.packages("data.table")